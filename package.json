{
  "name": "cottagelympics",
  "version": "1.0.0",
  "description": "A cottage-themed Olympics game with physics",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url ./",
    "predeploy": "npm run build",
    "deploy": "node deploy.js"
  },
  "homepage": "https://teemu.github.io/cottagelympics",
  "dependencies": {
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "parcel-bundler": "^1.12.5"
  }
} 