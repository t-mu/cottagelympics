{
  "name": "cottagelympics",
  "version": "1.0.0",
  "description": "A cottage-themed Olympics game with physics",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url ./ && cp -r public/assets dist/",
    "predeploy": "npm run build",
    "deploy": "node deploy.js"
  },
  "homepage": "https://t-mu.github.io/cottagelympics",
  "dependencies": {
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "parcel-bundler": "^1.12.5"
  }
}
